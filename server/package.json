{
  "name": "petpy-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Petpy thin backend — Replicate proxy for Memorial Garden core (background-removal cutout + image-to-video). No AWS.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "replicate": "^1.4.0"
  }
}
